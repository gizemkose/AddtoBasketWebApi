apiVersion: v1
kind: Service
metadata:
  name: {{ template "ciceksepeti.fullname" . }}
  labels:
    app: {{ template "ciceksepeti.name" . }}
    chart: {{ template "ciceksepeti.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "ciceksepeti.name" . }}
    release: {{ .Release.Name }}
